<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.blog.mapper.SysNoticeMapper">


    <insert id="insert">
        insert into sys_notice(type, title, action_text, article_title, article_id, operator_name, content,is_read,create_time)
        VALUES (#{type},#{title},#{actionText},#{articleTitle},#{articleId},#{operatorName},#{content},#{isRead},#{createTime})
    </insert>
</mapper>
