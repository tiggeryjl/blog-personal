<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.blog.mapper.SysRoleMenuMapper">
    <insert id="batchInsert">
        INSERT INTO sys_role_menu(role_id, menu_id, create_time) VALUES
        <foreach collection="roleMenuList" item="list" separator="," >
            (#{list.roleId},#{list.menuId},#{list.createTime})
        </foreach>
    </insert>
    <insert id="addMenu">
        INSERT INTO sys_role_menu(role_id, menu_id, create_time)
        VALUES (#{roleId},#{menuId},#{createTime})
    </insert>

    <delete id="deleteByRoleId">
        DELETE FROM sys_role_menu
        WHERE role_id =#{roleId}
    </delete>
    <delete id="deleteByMenuId">
        DELETE FROM sys_role_menu
        WHERE menu_id IN
        <foreach collection="menuIds" item="menuId" open="(" separator="," close=")">
            #{menuId}
        </foreach>
    </delete>
</mapper>