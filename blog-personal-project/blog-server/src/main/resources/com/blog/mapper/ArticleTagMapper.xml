<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.blog.mapper.ArticleTagMapper">
    <insert id="batchInsert">
        INSERT INTO article_tag(article_id, tag_id, create_time)
        VALUES
        <foreach collection="tagIds" item="tagId" separator=",">
            (#{articleId}, #{tagId},#{createTime})
        </foreach>
    </insert>

    <select id="selectRelationTagIds" resultType="java.lang.Long">
        SELECT DISTINCT article_id FROM article_tag
        WHERE tag_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    <select id="selectRelationArticleId" resultType="java.lang.Long">
        SELECT tag_id FROM article_tag WHERE article_id =#{id}
    </select>

</mapper>