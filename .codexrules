# 项目基础信息
项目根目录：blog-personal
模块划分：
1. blog-personal-project：SpringBoot3 后端项目，Java + MyBatis
2. vue-blog-management：Vue3 + Vite 管理后台前端
3. vue-blog-personal：Vue3 + Vite 用户展示前端

# 强制开发规范
1. 遵循现有项目代码风格，不要随意更换工具、依赖写法；新增代码和已有代码保持一致。
2. 修改功能优先同时兼顾后端接口、前端请求、页面组件，保证入参、返回字段命名统一。
3. 数据库实体、Controller、接口请求使用驼峰命名；数据库字段下划线。
4. 后端新增接口必须增加参数校验、基础注释；统一返回结果封装。
5. 前端使用现有axios请求封装，不要重新手写请求函数。
6. 改动多处文件时，分模块列出修改文件路径，清晰说明改动点。
7. 不删除原有可用代码，新增功能尽量解耦；如果需要重构先告知风险。
8. 忽略目录：target、node_modules、dist、.git相关文件，不要修改编译/依赖目录。

# 输出要求
提供代码时，标注完整文件路径；
如果改动较多，先输出整体方案，再分段给出代码；
写完简单说明如何测试验证功能。
